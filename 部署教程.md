# 手把手：把个人作品集网站部署到 GitHub Pages（小白版）

> 你不用敲任何命令。全程用「GitHub 网页」+「GitHub Desktop（图形软件）」点鼠标完成。

---

## 我已经帮你做好的准备（你不用动）

| 文件 | 作用 |
|---|---|
| `vite.config.js` 里加了 `base: './'` | 让网页用相对路径，部署到 GitHub 任何位置都不会裂图 |
| `.gitignore` | 自动忽略 `node_modules`、`dist`，不会把几百 MB 的临时文件传上去 |
| `.github/workflows/deploy.yml` | 一个「自动部署机器人」：你一上传代码，它就在云端帮你构建并发布网站 |

你只需要做两件事：**① 把代码传到 GitHub；② 在网页上打开一个开关。**

---

## 第一步：注册 GitHub 账号

1. 打开 https://github.com （用浏览器，推荐 Edge / Chrome）。
2. 点右上角 **Sign up**（注册），用邮箱注册一个账号。
3. 用户名（username）建议全英文小写，例如 `lingxianchong`，以后网站网址里会出现它。
4. 验证邮箱，注册完成。

---

## 第二步：安装 GitHub Desktop（图形界面工具）

1. 打开 https://desktop.github.com
2. 点 **Download for Windows**。下载完双击安装，一路点「下一步 / Next / Install」。
3. 安装好后打开它，用刚才注册的 GitHub 账号 **Sign in（登录）**。

> GitHub Desktop 是用来「把电脑里的文件夹上传到 GitHub」的工具，不用记命令。

---

## 第三步：把网站文件夹发布到 GitHub

> 你的网站文件夹在：
> `C:\Users\sterling\WorkBuddy\2026-09-07-15-30-19`

1. 打开 GitHub Desktop。
2. 顶部菜单 **File → Add Local Repository…**
3. 点 **Choose…**，选中上面的文件夹 `2026-09-07-15-30-19`，点「选择文件夹」。
4. 它会提示「这个文件夹还不是 Git 仓库」，下面有个按钮 **Create a Repository**（或 Initialize），点它。
5. 在弹出的窗口里：
   - **Name（仓库名）**：填英文，例如 `ling-portfolio`（这就是网址最后一段，别用中文、别用空格）。
   - **Git ignore**：选 **None**（我们已有自己的 `.gitignore`）。
   - 点 **Create repository**。
   - 如果窗口里有 **Default branch name**，一定填 **`main`**（小写）；没有看到就跳过，GitHub Desktop 现在默认就是 main。
6. 创建好后，左下角会出现一个 **Publish repository**（发布仓库）按钮（绿色），点它。
7. 在发布窗口里：
   - **Name** 保持刚才的仓库名。
   - **Description** 可空。
   - **⚠️ 千万不要勾 Keep this code private**（免费版 GitHub Pages 需要「公开仓库」才能上线，勾了私有就打不开网页）。
   - 点 **Publish repository**。
8. 等待上传完成（第一次可能要一两分钟，因为要传代码；`node_modules` 会被自动忽略，不会传）。

上传成功后，你的代码就已经在 GitHub 上了。

---

## 第四步：在 GitHub 网页打开「Pages 自动部署」开关

1. 用浏览器打开 https://github.com ，登录后点右上角你的头像 → **Your repositories** → 进入刚才的仓库。
2. 点仓库顶部的 **Settings（设置，齿轮图标）**。
3. 在左侧栏找到 **Pages**（在「Code and automation」分组下，可能需要点开它）。
4. 找到 **Build and deployment → Source**，下拉框选 **GitHub Actions**。
5. 页面可能会自动保存。这时部署已经启动。

> 解释：第三步只是把代码放上去；这一步是告诉 GitHub「用我写好的机器人（deploy.yml）自动把代码变成网页并发布」。

---

## 第五步：等待并打开你的网站

1. 仍在仓库页面，点顶部 **Actions** 标签（旗子/列表图标）。
2. 你会看到一个名为 **Deploy to GitHub Pages** 的任务在跑（黄色圆点 = 进行中）。
3. 等它变成 **绿色对勾**（一般 1–3 分钟）。如果一直是黄色，刷新页面看看。
4. 回到 **Settings → Pages**，顶部会出现一行字：
   > Your site is live at https://你的用户名.github.io/仓库名/
5. 点这个链接，就是你的线上网站了！🎉

---

## 以后怎么更新网站内容

1. 在本地改好代码（比如改文案、换图片）。
2. 打开 GitHub Desktop，底部「Summary」框里随便写几个字（例如「更新内容」），点 **Commit to main**。
3. 点右上角 **Push origin**（推送）。
4. 去 GitHub 的 **Actions** 看机器人跑完（变绿），网站就自动更新了（刷新网页即可）。

---

## 常见问题

**Q：网址打开是 404 / 空白？**
- 检查第三步是否把仓库设成了 **Public**（不能是 Private）。
- 检查第四步 Source 是否选了 **GitHub Actions**（不是 Deploy from a branch）。
- 确认仓库的主分支名是 **main**。如果是 **master**，打开仓库里的 `.github/workflows/deploy.yml`，把第 4 行的 `branches: [main]` 改成 `branches: [master]`，然后推送一次。

**Q：网页样式/图片裂了？**
- 确认 `vite.config.js` 里第一行有 `base: './'`（我已经加好了，别删）。

**Q：Actions 里任务报错红叉？**
- 点开红色任务看日志。最常见原因是分支名不对（见上）。其余问题把红色报错文字发给我，我帮你改。

**Q：想换网址里的仓库名？**
- 仓库名就是网址最后一段。改名要在 GitHub 网页仓库 **Settings → Repository name** 改，改完网址随之变化（之前那个旧链接会失效）。

**Q：GitHub Pages 收费吗？**
- 公开仓库完全免费。

---

## 整体流程一览

```
注册 GitHub  →  装 GitHub Desktop  →  发布文件夹(公开, 分支main)
     →  网页 Settings/Pages 选 GitHub Actions  →  等 Actions 变绿  →  打开网址
```
